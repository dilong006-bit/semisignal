# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
*.local

# vercel (vercel dev/link 가 만드는 로컬 프로젝트·org ID — 커밋 금지)
.vercel

# env — 실제 키가 들어가는 파일은 절대 커밋하지 않는다
.env
.env.*
!.env.example

# editor / OS
.vscode/*
!.vscode/extensions.json
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
